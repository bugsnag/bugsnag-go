version: '3.4'
services:
  gin-default:
    build:
      context: gin
      args:
       - GO_VERSION
       - GIN_VERSION
    ports:
      - "4511:4511"
    environment:
      - API_KEY
      - NOTIFY_ENDPOINT
      - SESSIONS_ENDPOINT
    restart: "no"
    command: go run main.go -case=default
